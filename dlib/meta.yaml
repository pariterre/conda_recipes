package:
  name: dlib
  version: "19.10.0"

source:
  git_url: https://github.com/pyomeca/dlib

build:
  number: 4

requirements:
  host:
    - lapack
    - libgcc

  build:
    # - {{ compiler('cxx') }}
    - cmake

about:
  home: https://github.com/pyomeca/dlib
  summary: Library for optimisation

extra:
    recipe-maintainers:
        - mrtnz
        - pariterre
